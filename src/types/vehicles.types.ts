export interface CreateVehicle {
  licensePlate: string;
  model: string;
  year: number;
  km: number;
}

export interface Vehicle {
  licensePlate: string;
  model: string;
  year: number;
  km: number;
}
